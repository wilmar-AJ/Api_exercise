<?xml version="1.0" encoding="utf-8" ?>
<Shell 
        x:Class="Api_exercise.Pages.AppShell"
        xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:pg="clr-namespace:Api_exercise.Pages"
        Shell.FlyoutBehavior="Flyout"
        Title="AppShell">

          <ShellContent
        Title="Productos"
        ContentTemplate="{DataTemplate pg:ProductsPage}"
        Route="ProductsPage"/>
          
          <ShellContent
        Title="Productos local"
        ContentTemplate="{DataTemplate pg:ProductsLocalPage}"
        Route="ProductsLocalPage"/>
</Shell>